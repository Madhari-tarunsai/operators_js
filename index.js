alert(`Now we will check the length of two numbers , which  are ( Greater ) or ( Equal ) or ( Smaller )`);
      let l1=prompt("enter the value of l1:")
      let l2=prompt("enter the value of l2:")

      let result;
      if(l1>l2){
        alert(`greater`)
      }
      else(l1<l2)
      {
        alert(`smaller`)
      }
      if(l1==l2)
        {
            alert(`equal`)
      }